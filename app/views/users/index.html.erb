<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<p id="notice"><%= notice %></p>

<h1><%= User.model_name.human %></h1>

<table>
  <thead>
  <tr>
    <th><%= User.human_attribute_name(:title) %></th>
    <th><%= User.human_attribute_name(:memo) %></th>
    <th><%= User.human_attribute_name(:author) %></th>
    <th><%= User.human_attribute_name(:picture) %></th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |f| %>
    <tr>
      <td><%= f.id %></td>
      <td><%= f.email %></t>
      <td><%= link_to t('views.common.show'), "/users/#{f.id}" %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @users %>

<br>

<%= link_to t('views.common.new'), new_book_path %>
